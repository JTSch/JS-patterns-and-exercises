// случайное число от [мин до макс)
alert( min + Math.random() * (max - min) );
// случайное целое число от мин до макс включ-но
var randV = min + Math.random() * (max - min) + (max - min) / 10;
    randV = Math.round(rand);
